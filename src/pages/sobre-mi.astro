---
import Layout from '../layouts/Layout.astro';
import Badge from '../components/Badge.astro';

const skills = [
  {
    category: "Lenguajes",
    items: ["HTML5", "CSS3", "JavaScript (ES6+)", "Python", "SQL"]
  },
  {
    category: "Frameworks & Librerías",
    items: ["Astro 🚀", "React", "Tailwind CSS", "Bootstrap", "Node.js"]
  },
  {
    category: "Herramientas & Otros",
    items: ["Git", "GitHub", "VS Code", "Figma", "Postman", "Terminal"]
  }
];
---

<Layout title="Sobre mí">
    <div class="container">
        <h1>Sobre mí 👨‍💻</h1>
        
        <div class="split">
            <div class="text">
                <section>
                    <h2>El lado Profesional</h2>
                    <p>
                        Llevo programando desde [Año]. Me especializo en el ecosistema de JavaScript, 
                        con un fuerte enfoque en el rendimiento y la accesibilidad. Me encanta resolver 
                        problemas complejos y optimizar código para que vuele.
                    </p>
                    <p>
                        Actualmente estoy buscando oportunidades donde pueda aportar mi experiencia en 
                        <strong>Astro, React y Node.js</strong>.
                    </p>
                </section>

                <section>
                    <h2>El lado Personal</h2>
                    <p>
                        Cuando no estoy delante de una pantalla, me encontrarás haciendo senderismo, 
                        leyendo ciencia ficción o intentando hacer la pizza perfecta. Creo que el equilibrio 
                        vida-trabajo es clave para mantener la creatividad.
                    </p>
                </section>
            </div>
        </div>
        <section class="skills-section">
            <h2>🛠️ Mi Stack Tecnológico</h2>
            <p class="skills-intro">Estas son las tecnologías con las que trabajo habitualmente:</p>
            
            <div class="skills-grid">
                <!-- Recorremos las categorías -->
                {skills.map((group) => (
                    <div class="skill-card">
                        <h3>{group.category}</h3>
                        <div class="badges-container">
                            <!-- Recorremos los items de cada categoría -->
                            {group.items.map((item) => (
                                <Badge>{item}</Badge>
                            ))}
                        </div>
                    </div>
                ))}
            </div>
        </section>
    </div>
</Layout>

<style>
    h1 { font-size: 3rem; margin-bottom: 2rem; }
    h2 { color: #4f39fa; margin-top: 2rem; }
    p { line-height: 1.6; color: #d1d1d1; margin-bottom: 1rem; }
    
    .split {
        display: flex;
        gap: 3rem;
        align-items: flex-start;
    }
    .text { flex: 2; }
    .image-container { flex: 1; }
    
    .about-img {
        width: 100%;
        border-radius: 12px;
        transform: rotate(3deg);
        border: 2px solid #444;
    }

    @media (max-width: 768px) {
        .split { flex-direction: column-reverse; }
        .about-img { transform: rotate(0); width: 200px; margin: 0 auto; display: block; }
    }

    .skills-section {
        margin-top: 4rem;
        padding-top: 2rem;
        border-top: 1px solid #333; /* Una línea separadora sutil */
    }

    .skills-intro {
        color: #aaa;
        margin-bottom: 2rem;
    }

    .skills-grid {
        display: grid;
        /* Esto crea columnas automáticas. Si cabe una, pone una. Si caben tres, pone tres. */
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
    }

    .skill-card {
        background: #23262d;
        padding: 1.5rem;
        border-radius: 12px;
        border: 1px solid #333;
        transition: transform 0.2s;
    }

    .skill-card:hover {
        border-color: #555;
        transform: translateY(-5px); /* Pequeño salto al pasar el ratón */
    }

    .skill-card h3 {
        margin-top: 0;
        margin-bottom: 1rem;
        color: #4f39fa; /* Color morado para los títulos */
        font-size: 1.2rem;
    }

    .badges-container {
        display: flex;
        flex-wrap: wrap; /* Permite que los badges bajen de línea si no caben */
        gap: 0.5rem; /* Espacio entre badges */
    }
</style>