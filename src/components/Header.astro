<header class="header">
    <nav>
        <!-- Logo / Nombre a la izquierda -->
        <a href="/" class="logo">Carlos Merino</a>
        
        <!-- Enlaces a la derecha -->
        <div class="nav-links">
            <a href="/">Inicio</a>
            <a href="/proyectos">Proyectos</a>
            <a href="/sobre-mi">Sobre mí</a>
            <a href="/contacto">Contacto</a>
            
            <!-- Botón destacado de CV -->
            <a href="/cv.pdf" target="_blank" class="btn-cv">
                Descargar CV
            </a>
        </div>
    </nav>
</header>

<style>
    .header {
        background-color: #1a1a1a; /* Fondo oscuro */
        border-bottom: 1px solid #333; /* Línea sutil abajo */
        position: sticky; /* Se queda pegado al hacer scroll */
        top: 0;
        z-index: 100; /* Asegura que flote por encima del resto de cosas */
        padding: 0.5rem 0;
    }

    nav {
        display: flex;
        justify-content: space-between; /* Logo a un lado, enlaces al otro */
        align-items: center;
        max-width: 1000px; /* Un poco más ancho que el texto para que respire */
        margin: 0 auto;
        padding: 0 1.5rem;
        height: 60px;
    }

    .logo {
        font-weight: 800;
        font-size: 1.5rem;
        color: white;
        text-decoration: none;
        letter-spacing: -1px;
    }

    .nav-links {
        display: flex;
        align-items: center;
        gap: 1.5rem; /* Espacio entre cada enlace */
    }

    .nav-links a {
        color: #ccc;
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 500;
        transition: color 0.3s ease;
    }

    /* Efecto hover para enlaces normales */
    .nav-links a:hover {
        color: white;
    }

    /* ESTILOS DEL BOTÓN CV */
    .btn-cv {
        background-color: white;
        color: #1a1a1a !important; /* Texto oscuro forzado */
        padding: 0.4rem 0.8rem;
        border-radius: 999px; /* Bordes totalmente redondos */
        font-weight: 700 !important;
        font-size: 0.9rem !important;
        transition: transform 0.2s, background-color 0.2s;
    }

    .btn-cv:hover {
        background-color: #e0e0e0;
        transform: scale(1.05); /* Crece un poquito al pasar el ratón */
    }

    /* RESPONSIVE: Ajustes para pantallas pequeñas (Móvil) */
    @media (max-width: 768px) {
        nav {
            flex-direction: column; /* Pone el logo arriba y menú abajo */
            height: auto;
            padding: 1rem;
            gap: 1rem;
        }
        
        .nav-links {
            gap: 1rem;
            flex-wrap: wrap; /* Permite que bajen de línea si no caben */
            justify-content: center;
        }

        .btn-cv {
            margin-left: 0; /* Quita márgenes extra en móvil */
        }
    }
</style>